# CLAUDE.md — Casual PDF

## What this repo is

Solo / personal project named **Casual PDF** — a production-grade, high-fidelity **PDF viewer + editor** with real-time co-editing, e-signing, public share links, and granular reading/editing rights. Web, desktop, and embeddable. The path contains `melp/` as a **folder name only** — not a company or product. Do not call this project "melp" or imply organizational context.

**As of 2026-06-27 this repo holds design/planning docs only — no code yet, intentionally.** The plan is reuse-first: lean on existing sibling repos (`collab`, `desktop`, `design-system`) and MIT/Apache/BSD open source, not a from-scratch PDF engine.

## Required reading before substantive work

1. [`README.md`](./README.md) — vision + the single engine decision.
2. [`docs/ARCHITECTURE.md`](./docs/ARCHITECTURE.md) — system design, document model, Rust core, collab/desktop wiring.
3. [`docs/RESEARCH.md`](./docs/RESEARCH.md) — OSS building blocks + license table + competitive landscape.
4. [`docs/BENCHMARK.md`](./docs/BENCHMARK.md) — the competitive bar + testable UX/perf acceptance gates (UX-*).
5. [`docs/FEATURES.md`](./docs/FEATURES.md) — every must-have mapped to reuse / OSS / build.
6. [`docs/ROADMAP.md`](./docs/ROADMAP.md) — phased plan; Phase 0 is the first concrete step.

## Architecture in one breath

```
UI (React 18 + Vite + TS, @schnsrw/design-system)
  └─ @casualoffice/pdf SDK
       ├─ Viewer: EmbedPDF headless (PDFium-WASM)   ← render/scroll/zoom/search
       ├─ Document model: Yjs (annotations/forms/comments/signing) over immutable base bytes
       ├─ Write-side: pdf-lib + @signpdf/signpdf (stamp/forms/PKCS#7)
       └─ Heavy ops: crates/casual-pdf-core (Rust) → WASM (web worker) + native (desktop)
  ⇄ services/collab (Yjs/Hocuspocus) — co-editing, presence, public links, rights, blob host
  ⇄ services/desktop (Tauri 2) — offline, chunked atomic save, crash recovery, native print-to-PDF
```

## Locked decisions (do not relitigate without explicit user direction)

1. **PDFium is the single engine everywhere.** Web = PDFium-WASM via **EmbedPDF** (MIT, headless). Desktop = same PDFium native via **`pdfium-render`** (Apache/MIT). Heavy worker = one Rust crate (`crates/casual-pdf-core`) compiling to **both** `wasm32` and native. Identical fidelity → gate **UX-F1**. Don't introduce a second renderer (no PDF.js as the primary engine; it's a reference/fallback only).
2. **CRDT overlay, immutable base.** Editable state (annotations, form values, comments, signing) lives in a **Yjs** doc; the PDF page bytes for a version are immutable, content-addressed blobs. **Never try to CRDT-merge raw PDF bytes.**
3. **Yjs is the chosen CRDT.** Do not propose Automerge / Loro / custom alternatives.
4. **MIT / Apache-2.0 / BSD only. No (A)GPL anywhere in the shipped artifact.** Specifically **MuPDF / mupdf-rs are excluded (AGPL).** Approved: EmbedPDF, PDFium, pdf-lib, pdfme, lopdf, pdfium-render, @signpdf/signpdf, node-forge (use under BSD-3), Yjs, Hocuspocus, PDF.js (Apache, reference only). **Extension (user direction 2026-07-04): OFL-1.1 (SIL Open Font License) is permitted for FONT assets specifically** — it's the standard permissive, non-viral font license built for embedding/redistribution. Bundled metric-compatible fonts for text editing (Arimo, then Tinos/Cousine/Carlito/Caladea) ship under OFL-1.1 with a NOTICE entry. This does NOT loosen the MIT/Apache/BSD rule for code dependencies.
5. **Reuse `services/collab` as-is.** It's document-agnostic Yjs/Hocuspocus already powering Docs/Sheets. New sync/presence/share-link/persistence work goes there, not a new server. Set `CASUAL_FILE_EXT=.pdf`; extend the role set to `{viewer, commenter, editor, signer}`.
6. **Reuse `services/desktop` (Tauri 2).** Adding this app is the documented convention: extend `DocKind` with `Pdf`, mount `/pdf/index.html`, ship a `desk-bridge-bootstrap.ts`, and expose native Rust-core commands via Tauri `invoke`. Don't rebuild file I/O — chunked atomic save + recovery already exist in the shell.
7. **Standalone repo, built-in to desktop.** `casual_pdf` stays its own repo (like `document`/`sheet`); the desktop shell's `copy-editors.sh` pulls its `dist/` into `public/pdf/`. Mirror the `@casualoffice/sheets` SDK pattern → publish `@casualoffice/pdf` with subpath exports (`/viewer`, `/collab`, `/embed`).
8. **v1 = Tier 1 full editor.** Annotate, forms, e-sign + certified PKCS#7, redaction (true byte-level removal), page ops, co-editing, public links, rights. **Tier 2** (true body-text editing with reflow) is a later stretch — PDF isn't a reflowable format; it's Adobe's moat. Do **not** market or scope Tier 1 as Tier 2.
9. **One core × 3 surfaces × 3 modes × collab on/off — same codebase.** Surfaces = desktop / web / SDK. Modes = **View** (read-only) / **Edit** (direct) / **Suggest** (proposals an owner accepts/rejects, Google-Docs-style). Collab **off** = solo with local persistence (`y-indexeddb` / desktop sidecar); collab **on** = co-editing via `services/collab`. Mode and collab are **independent runtime flags on the same `@casualoffice/pdf` core** — never separate builds or per-mode forks. Suggest = Yjs entries tagged `state: suggested` reviewed to `applied`/removed. Rights map to modes and are server-enforced. See `docs/ARCHITECTURE.md` §2b.

**Repo license:** this repository is **Apache-2.0** (`LICENSE` + `NOTICE`). (Separately, decision #4 governs which *dependency* licenses may be bundled: MIT/Apache/BSD only.)

## Working rules

1. **Never write technical claims about external systems (collab/desktop/design-system, OSS libs) from memory.** Read the actual source first; cite file paths (`services/collab/src/...:LINE`).
2. **Every feature ships against its UX-* gate** in `docs/BENCHMARK.md`. A feature isn't "done" until its referenced gate passes — automate the test where possible (perf budget, screenshot-diff for UX-F1, Acrobat-validation fixture for UX-S2, text-extract assertion for UX-S5).
3. **Frontend stack is React 18 + Vite + TS**, matching `services/document` and `services/sheet`. UI from `@schnsrw/design-system` (tokens + components). Don't introduce a different framework.
4. **Rights are enforced server-side at the collab room**, not just hidden in the UI (gate UX-S4). Viewers/commenters get a read-only Yjs connection.
5. **Edits use incremental update** (append, don't rewrite) to preserve originals and enable signatures (gate UX-F2).
6. **Don't install software via `curl | bash` from a remote URL without explicit user consent.** Prefer Homebrew / npm / cargo; ask which install method first.
7. **Docs are first-class.** When a doc-tracked fact changes (a locked decision, status, gate, phase state), update the relevant doc in the same commit. Stale docs poison every future session.

## Where things will live (target layout — see ARCHITECTURE.md §8)

- `apps/web/` — Vite React SPA: viewer/editor UI + `desk-bridge-bootstrap.ts`.
- `packages/pdf-sdk/` — `@casualoffice/pdf` embeddable SDK (mirror `@casualoffice/sheets`).
- `crates/casual-pdf-core/` — Rust (pdfium-render + lopdf); `crate-type = ["cdylib","rlib"]`; wasm + native.
- `collab/` — thin config/override over `services/collab` (or import directly).
- `docs/` — the planning docs above; keep current.

## Status (2026-06-27)

- **Planning complete.** README + `docs/{RESEARCH,BENCHMARK,ARCHITECTURE,FEATURES,ROADMAP,OVERVIEW}.md` written.
- **Decisions locked** (see above): PDFium-everywhere, CRDT-overlay, reuse collab/desktop/design-system, standalone repo, v1 = Tier 1.
- **Phase 0 scaffold built & verified.** `apps/web` (Vite+React+TS), `packages/pdf-sdk` (`@casualoffice/pdf`), `crates/casual-pdf-core` are in place. Verified green: JS typecheck + web build (bundles real **PDFium-WASM** via EmbedPDF — engine spike compiles & bundles), Rust `fmt`/`clippy -D warnings`/`test`, and **one crate → both `wasm32` + native** targets. `@schnsrw/design-system` wired as a `vendor/` git submodule (`link:` pnpm override, like `sheet`); App toolbar uses its `Button` + `tokens.css`. Deploy is **live on push to `main`** at **pdf.casualoffice.org** (repo var `PAGES_CUSTOM_DOMAIN` set; CI checkout fetches submodules).
- **UX-F1 (render parity) green.** `tools/render-parity/` diffs a fixture page rendered by web PDFium-WASM (EmbedPDF, via the app's `?src=` override + Playwright) against native `pdfium-render` (`--example render_page`, libpdfium from bblanchon/pdfium-binaries); **diff 0.217% < 2%** threshold. Wired as a CI job (`render-parity`). Runtime web render also verified live (UX-P1/P2 basic).
- **Deferred (tracked in ROADMAP):** SDK `peerDependencies` for `yjs`/`@hocuspocus/provider` + `/viewer`,`/collab`,`/embed` subpath exports (Phase 6 publish); collab `signer` role + write-gating (Phase 3/4); desktop `DocKind::Pdf` + `copy-editors.sh` `/pdf` (Phase 1); outline/bookmarks panel + continuous/single scroll-mode toggle (Phase 1).
- **Phase 1 production viewer (in progress).** `packages/pdf-sdk` now layers a full viewer on the engine via EmbedPDF plugins: virtualized scroll + tiling, zoom/fit-width/fit-page, rotate, page nav, two-page spread, pan, fullscreen, text search + highlights, text selection, thumbnails — driven by a floating toolbar (`src/ui/chrome.tsx`, `icons.tsx`, `IconButton.tsx`, `viewer.css`). UI follows the design prefs: SVG icons (filled=active), WCAG 2.2, rounded/floating surfaces with color-separation (design-system tokens), light/dark. Verified headless (4-page nav, zoom 140%, 19 search matches, thumbnails, dark). Note: `@embedpdf/plugin-selection`/`-pan` require `@embedpdf/plugin-interaction-manager` registered. See [[feedback-ui-design-prefs]].
- **Layout = professional PDF editor (Acrobat/Nutrient-style), NOT Google Docs.** (User reversed off Docs on 2026-06-28; **fidelity + editing UX prioritized over collaboration**.) `apps/web` slim top bar: hamburger menu (Open/Download/Print/About) + logo + editable title + **View/Suggest/Edit segmented control** + theme + avatar. SDK `src/ui/chrome.tsx`: **left tool rail** (nav + annotation tools + undo/redo, Edit/Suggest only), **right contextual properties panel** (color/width/delete), **floating bottom view bar** (nav/zoom/fit/rotate/spread/scroll-dir/pan/fullscreen), max canvas, find fab top-right. Keyboard shortcuts (V/H/D/T/N/R/O/A, Esc, Delete) + crosshair cursor with a tool active. Brand **logo + favicon** from `apps/web/public/logo.svg`. iLovePDF added to `docs/RESEARCH.md`. See [[feedback-ui-design-prefs]].
- **App shell: File/View/Help menu bar + Open/Save (done).** `apps/web/src/Menu.tsx` (accessible Google-Docs menu bar) + `App.tsx`: **Open** a local PDF (file picker → object URL → `key`-remount the viewer), **Download**, **Print/open-in-tab**, ⌘O/⌘S, dark-theme (View menu, app-owned via `data-theme`), full screen, About dialog. **Outline/bookmarks** panel + **scroll-direction** toggle added to the SDK viewer (one left panel at a time). Theme moved out of the toolbar into the View menu.
- **Phase 2 annotation editor (in progress).** `@embedpdf/plugin-annotation` + `-history` + `-export` wired: tool palette (highlight, ink, free-text, note, rectangle, ellipse, arrow) in the toolbar (Edit/Suggest only) + the **Insert** menu; undo/redo (toolbar + Edit menu); File → Download bakes annotations (export plugin). Key gotchas: each page must wrap its layers in `PagePointerProvider` (pointer routing), the viewer must be wrapped in `AnnotationRendererProvider`, and `AnnotationLayer` needs an explicit fill `style` (`position:absolute; inset:0`) or it collapses to 0-height. SDK exposes an imperative `CasualPdfApi` via `apiRef` (download/undo/redo/deleteSelection/setTool) for host menus. Text `SelectionLayer` renders only in view mode (annotation tools own the pointer in edit mode). See [[feedback-ui-design-prefs]].
- **Tier-1 editor features shipped (Edit/Suggest, left rail + menus).** **Organize Pages** (reorder/delete via `engine.mergePages` → `openDocumentBuffer`); **Form filling** (`@embedpdf/plugin-form`, interactive AcroForm fields via `AnnotationLayer annotationRenderers={formRenderers}`); **E-signature** (`@embedpdf/plugin-signature`, SignatureOnly mode — a "Sign" rail tool opens a Draw/Type modal; "Add signature" arms placement, a banner prompts a page click; placements are signatureStamp/signatureInk annotations rendered by the AnnotationLayer and **baked into the PDF on Download** via appearance streams). All verified headless end-to-end (e-sign confirmed in the downloaded bytes by re-opening the file). **Note:** e-signature here is a *visible* signature stamp, **not** certified cryptographic PKCS#7 signing (that's still a v1 item per locked decision #8 — UX-S2).
- **Certified PKCS#7 signing FIXED to be real + verifiable, and a signature verifier/details panel added (UX-S2, 2026-07-04).** A user report ("shows Signed but no cryptographic details") exposed that signing had regressed to a **broken Rust-wasm signer** (`crates/casual-pdf-core/src/sign.rs`): a **hardcoded private key** (`SigningKey::from_bytes([1u8; 32])` — everyone shared one constant key) and **`certificates: None`** (no X.509 cert embedded) → every signature was **unverifiable by any tool** (Acrobat included). **Fix:** `packages/pdf-sdk/src/sign.ts` now signs entirely in the in-policy JS stack — `@signpdf/signpdf` + `@signpdf/placeholder-plain` + `@signpdf/signer-p12` + `node-forge` (decision #4), as an **incremental update** (original bytes preserved → decision #5 / UX-F2). It mints a **real self-signed identity** (RSA-2048 via WebCrypto keygen — fast, non-blocking — + a forge-built X.509 cert), **persisted per signer-name in `localStorage`** (stable identity; user decision 2026-07-04), and `@signpdf` **embeds the cert** so the signature actually verifies. The Rust wasm signer is now unused by the flow. **New read-side verifier:** SDK subpath `@casualoffice/pdf/verify` (`packages/pdf-sdk/src/verify.ts`, lazy) parses each `/ByteRange`+`/Contents` PKCS#7, **verifies the content digest** (messageDigest == SHA-256 of the ByteRange bytes → document intact) **and the RSA signature** over the signed attributes, reads signer/issuer/validity/signing-time, and flags self-signed + not-whole-document. App: the app-bar signature **badge is now a button** → `SignatureInfoDialog` (`apps/web/src/SignatureInfoDialog.tsx`) shows the verdict + details; it verifies the **pristine `fetch(src)` bytes** (a PDFium re-export via `getBytes()` shifts offsets and breaks the byte-exact ByteRange). Two subtle gotchas fixed: forge's `asn1.fromDer` needs `{parseAllBytes:false}` (the /Contents placeholder is zero-padded), and hashing must use `forge.util.binary.raw.encode` — **not** `TextDecoder('latin1')`, which decodes as windows-1252 in browsers (0x80–0x9F remap) and corrupts the digest. **Verified headless e2e** (`tools/render-parity/verify-signature-details.mjs`): sign → reload → badge "Certified" → dialog reports **valid, digest matches, signature cryptographically valid, self-signed caveat**. **Still a v1 item:** uploading your own CA-issued **.p12/.pfx** for a *verified* (not self-asserted) identity.
- **Redaction true-removal SHIPPED via rasterize-and-flatten (UX-S5 green).** Root cause of the earlier failure found: PDFium **object editing** (`FPDFPage_RemoveObject`) segfaults on the available builds — confirmed natively with `pdfium-render` (rendering on the *same* libpdfium works fine; only object removal crashes), so the EmbedPDF PDFium-WASM build has the same broken path. Chosen approach (user decision 2026-06-28): a **Redact** rail tool marks regions (fractional page coords, drag-to-mark red overlay via `RedactionLayer` in `src/ui/chrome.tsx`); **Apply** renders each marked page at 2× with annotations baked (`useRenderCapability().forDocument(id).renderPage`), paints opaque black boxes on a canvas, and rebuilds the PDF with **pdf-lib** (`src/redact.ts`, `buildRedactedPdf`) — redacted pages become flattened images, untouched pages are `copyPages`'d verbatim (keeping their text) — then reloads via `openDocumentBuffer`. True removal by construction: the content isn't in the output bytes. **Trade-off:** a redacted page loses selectable/searchable text (it's an image). pdf-lib is lazy-loaded (own `redact-*` chunk, ~90 KB gz). Verified headless: search "quick" 1/1 before → **0/0 after**; black box rendered. Also **redact-by-selection**: the selection mini-toolbar has a Redact action that converts the selection's per-line `segmentRects` (page points → fractions via `docCap.getDocument(id).pages[i].size`) into redaction marks — verified: select body text → 6 per-line marks → Apply → "parity" 7→0, tight per-line black boxes. And **redact search matches**: the find bar has a "Redact all N matches" action (Edit/Suggest) that marks every search hit's rects — verified: search "parity" (7) → Redact all → 7 marks → Apply → 0/0, each occurrence precisely blacked out (Acrobat-style word-level redaction). The surgical lopdf path was attempted but **REVERTED** — see the next bullet.
- **Redaction redo — flatten-only, geometry-preserving (2026-06-30, research-driven).** A user test on a real doc found redaction badly broken: a small redaction wiped most of a page and rotated/repositioned content ("horizontal became vertical; bled content snapped inside"). A deep-research pass (web, adversarially verified) showed: (a) rasterize-and-flatten is the **secure** method (immune to the de-redaction attacks — preserved glyph-advance widths, sub-pixel neighbour shifts — that defeat surgical text removal, per arXiv:2206.02285); (b) my **surgical wasm path was unsafe**: it didn't descend into Form XObjects and couldn't handle Type3/Type0 fonts → it *under-redacted* (left text in the bytes), and its advance-width-preserving `TJ` rebuild is itself a de-redaction vector. User decision: **flatten is the sole redaction path; surgical is shelved** until it's a fail-closed interpreter (a later opt-in). **The bug:** `buildRedactedPdf` rebuilt each page from the rendered image's pixel dims with origin (0,0) and no rotation → discarded MediaBox origin, CropBox, and `/Rotate`. **The fix** (`packages/pdf-sdk/src/redact.ts`): the rebuilt flattened page now inherits the source page's **MediaBox (origin-aware), CropBox, and `/Rotate`** (pdf-lib getMediaBox/getCropBox/getRotation → setMediaBox/setCropBox/setRotation); pages render in **native** orientation (renderPage default — verified the viewer displays/renders native, so marks paint directly, no rotation math); untouched pages copied verbatim. Confirm-dialog copy corrected (redacted pages become flattened images; text on them no longer selectable; untouched pages keep text). `applyRedactions` no longer calls the surgical wasm. **Verified** with a `/Rotate-90` + non-zero-MediaBox-origin fixture (`tools/render-parity/fixtures/geom.pdf`, gen via `make-geom-fixture.mjs`): `tools/render-parity/verify-redact-geom.mjs` asserts the output keeps Rotate 90 + origin 36, the redacted page's text is gone (UX-S5), an untouched page keeps its text, and a screenshot confirms the box lands where dragged. The shelved surgical code (`crates/casual-pdf-core/src/redact.rs`, `redact-core.ts`, `redact_pdf_wasm`) remains but is unused by the redaction flow. **Open follow-up:** in-app text **search** returns 0 on any `openDocumentBuffer`-reopened doc (pre-existing; selection + output bytes unaffected).
- **Phase 2 finished (marquee multi-select + autosave/recovery).** **Marquee** (UX-I3): `MarqueeSelect` in `chrome.tsx` — rubber-band drag over empty canvas in Select mode selects every enclosed annotation (feeds the existing multi-select machinery); cleanly yields to *text* selection when a drag starts on glyphs (watches `getFormattedSelection`). **Autosave/recovery** (UX-I5): host-owned in the web app — `apps/web/src/recovery.ts` debounce-snapshots the exported bytes to IndexedDB; on load a banner offers Restore/Discard (`App.tsx`); cleared on a clean Download. Full-bytes snapshot (not an overlay diff) so it survives a tab crash even for picker-opened files. Both **verified headless** (`verify-drive.mjs`). The Yjs *binding* (source-of-truth swap) stays **deferred to Phase 3** — collab is deprioritized; the model is already defined in `model.ts` and recovery rides on the bytes-snapshot.
- **Insert Image shipped (Edit/Suggest, left rail).** An "Image" rail tool picks a PNG/JPEG, reads its bytes + natural aspect, and arms placement (a banner prompts a page click); the click drops it as an image **STAMP** annotation (`createAnnotation(pageIndex, {type: STAMP=13, …}, { data, mimeType })` — the same stamp/appearance-stream path as e-signature image stamps) that renders via the AnnotationLayer and **bakes into the PDF on Download**. Verified headless: placed on page, download grew, and re-opening the file fresh shows the image is in the bytes. See `ImagePlacer` in `src/ui/chrome.tsx`.
- **Production-hardening pass done (2026-06-29).** A full code + UX review (two review agents) drove fixes, prioritising the redaction *trust* path: a failed page-render during Apply no longer silently leaves content un-redacted (hard-fails + surfaces the error, keeps marks); redaction forces native orientation (rotation 0) so marks can't misalign with the rendered bitmap; flattened pages are sized to the rendered image (handles intrinsic `/Rotate`); a **confirmation dialog** discloses the permanent flatten-to-image before applying. Edit-state teardown: leaving to View tears down all editing surfaces (pending image, redaction marks, organize/signature modals, armed signature `deactivatePlacement`); full-screen no longer wipes in-progress marks (destructive cleanup keys on `mode==='view'`, not `editing`). Data-loss guards: SDK `onEdited` → app dirty flag → confirm before Open/Open-sample + `beforeunload`. Plus: copy/paste/duplicate skip image stamps (blank-clone), Organize gains Escape + error surfacing, SignatureModal traps focus + restores it, scroll-direction toggle resets on doc swap, signature pad forced light for dark-mode contrast. All verified headless.
- **"Quick text edits" — single-run in-place edit (engine + UI), 2026-06-30; NOT Tier-2. (Reassessed 2026-07-04.)** Engine = **PDFium text-object editing** (FPDFText_SetText → FPDFPage_GenerateContent → FPDF_SaveAsCopy), NOT the lopdf Tj-rewrite. **Web needs no Rust and no 2nd PDFium:** the PDFium-WASM EmbedPDF already exports the edit API. UI: an **Edit text** rail tool (`TextEditLayer` in chrome.tsx); runs grouped by baseline sort, inline inputs matched to PDF font/size/color. Wasm fetched from a locally-bundled asset (hashed, HTTP-cached). Desktop/Tauri-webview uses the same path. **Honest scope (do NOT market as "true in-place editing" / Tier-2 — a multi-agent assessment on 2026-07-04 rated it demo-grade):** it edits ONE reconstructed run at a time, **substitutes a standard-14 font on nearly every real (subsetted) document** (typeface changes), **does not reflow** the paragraph, and is **not a secure removal tool** (a suppressed run can be moved off-page and remain extractable — use Redaction to remove). Tier-2 (true reflow + font re-subsetting) remains a later stretch per locked decision #8. **Trust-hardening (2026-07-04):** substitution now **fails closed** on characters WinAnsi can't encode (CJK/symbols) instead of silent tofu (`firstUnencodable` in `textedit-pdfium.ts`); `editTextRun` returns a `residual` flag when an off-page fallback left original glyphs in the bytes, and the banner warns "…use Redaction to remove"; the ignored `FPDFText_SetText` result on the substituted object is now checked; in-product copy reframed to "Quick text edits". **Open trust caveats:** the full-rewrite save (`FPDF_NO_INCREMENTAL`, chosen to avoid burying old text) **invalidates any pre-existing PKCS#7 signature** — inherent tension with removal; warn-on-signed-doc is a follow-up. See the assessment in the 2026-07-04 workflow.
- **Overlay-replace + Secure/bake, then Option C matched-font (2026-07-04, "quick edits done right").** Per [`docs/TEXT-EDITING.md`](./docs/TEXT-EDITING.md): the **Edit text** tool has an opt-in **Overlay** mode — cover the old run + retype on top via **pdf-lib** (`textedit-overlay.ts`), **no content-stream rewrite** (so no reflow/justification damage; direct edit stays the default searchable path). A **Secure** sub-toggle **flattens** the edited page (canvas composite → `buildRedactedPdf`) so the original text is **truly removed** (`flattenPageWithOverlay` in chrome.tsx). Both sample the page background so the cover box blends. **Option C (PR1):** the overlay now embeds a **bundled metric-compatible font** so edits **keep the apparent typeface** instead of a standard-14 substitute — `textedit-fonts.ts` matches the run's `fontBaseName` (**Arial/Helvetica → Arimo**, OFL-1.1, vendored at `packages/pdf-sdk/src/fonts/`) and `buildOverlayEdit` embeds it via `pdf-lib` + **`@pdf-lib/fontkit`** (`subset:true`, catch→`subset:false`); `substituted` is false when a font matched (no swap warning). The matched text is a Type0/CID font **with `/ToUnicode`** → searchable in compliant readers (the naive `extract-text.py` can't decode CID glyphs — test asserts the embedded `Arimo` instead). **Option C (PR2, 2026-07-04):** the matcher is now **weight+italic-aware** (`matchFont(baseName, weight, italic)`) with a per-family face map, and adds **Calibri → Carlito** (OFL-1.1, **static regular/bold/italic/bold-italic** — proper weights, unlike variable Arimo which falls back bold→regular). **Option C (PR3, 2026-07-04) — common Office set COMPLETE:** added **Times → Tinos, Courier → Cousine, Cambria → Caladea** (all OFL-1.1, static full-weight). Matcher normalizes real PostScript names (e.g. `TimesNewRomanPS-BoldMT` → Tinos-Bold). **Coverage now:** Arial→Arimo, Calibri→Carlito, Times→Tinos, Courier→Cousine, Cambria→Caladea; Verdana/Georgia + exotic/proprietary faces fall back to the standard-14 substitute (disclosed). **Remaining follow-ups:** static Arimo instances for true Arial-bold; matched font for the bake canvas path; `queryLocalFonts` for the genuine installed face. Verified headless: `verify-textedit-overlay`, `-optionc` (matched font embedded), `-overlay-bake` (original removed); family-match + weight-selection logic unit-checked (incl. Verdana→fallback).
- **Option C (PR4, 2026-07-04) — dynamic Google Fonts (curated).** Beyond the 5 bundled metric-compatible families, `matchFont` now resolves ~18 common **Google Fonts** (Roboto, Open Sans, Lato, Montserrat, Poppins, Inter, Raleway, Nunito, Noto Sans/Serif, Work Sans, Source Sans 3, Playfair Display, Ubuntu, Oswald, Rubik, DM Sans, Merriweather) by **fetching their TTF on demand** from the `google/fonts` repo via jsdelivr (OFL-1.1, not bundled — decision #4's font extension; NOTICE updated). So editing a document that actually uses one of those keeps its real typeface. `GOOGLE_FAMILIES` in `textedit-fonts.ts` holds **verified exact paths** — GF filenames are inconsistent (static vs `[wght]` vs `[wdth,wght]` vs `[opsz,wght]`) so they were each probed; variable-only families expose regular (+ italic where a separate variable file exists), bold falls back to regular. The existing `matchFont → fetchFontBytes → matchedFontBytes` caller is unchanged (its try/catch degrades a fetch failure to the standard substitute). Key gotcha confirmed: pdf-lib **can** embed both variable and static jsdelivr TTFs (`FontFile2` present — the earlier woff2 failure was brotli-glyf, not TTF). Verified headless (`verify-textedit-googlefont.mjs`): editing a Roboto run → the output embeds Roboto with **no `/Helvetica` substitute**. **Follow-ups:** the JS **desktop** path can read the genuine installed system face (fontdb via Tauri) for even better fidelity; Roboto Mono / PT Sans paths weren't resolvable in the probe.
- **Option C (PR5, 2026-07-04) — Unicode text editing + desktop genuine fonts.** Fixes a user-reported gap ("not supporting unicode char on text editing"). The **direct** (PDFium) edit path can only encode **WinAnsi** → it fails closed on CJK / non-Latin. New: a commit with characters WinAnsi can't encode (`firstUnencodable` != null, now exported) **auto-routes to the overlay path**, which embeds a covering font. Font selection moved to `resolveEditFont(baseName, weight, italic, text, needsUnicode)` in `textedit-fonts.ts`, tried in order: (1) the **genuine installed system font** on desktop (`window.__deskApp__.resolveSystemFont` → the shell's `fontdb`-backed `resolve_system_font`, base64→bytes), (2) the bundled / Google-Font metric match, (3) a **broad Noto fallback** — **Noto Sans** for most scripts, **Noto Sans SC** for CJK (both OFL-1.1, fetched on demand). Each candidate is **coverage-checked with fontkit** (`hasGlyphForCodePoint`) before use, so it never embeds a tofu-ing font; the bundled faces already cover Latin/Cyrillic/Greek, so Noto is only reached for CJK/uncommon scripts. `chrome.tsx` shows an honest note when the auto-overlay path is used (original text remains beneath → use Redaction/Secure). **Cross-repo:** the desktop `resolve_system_font` command lives in `services/desktop` (`apps/shell/src-tauri/src/fonts.rs`, `cargo check` green; TrueType Collections skipped — pdf-lib can't embed one face from a `.ttc`). Verified headless (`verify-textedit-unicode.mjs`): a CJK edit in direct mode **applies instead of failing** and embeds a `CIDFontType2` (Noto Sans SC); `-overlay`/`-optionc`/`-googlefont` still green.
- **Text-edit (and all edit tools) now persist across a commit — Viewer no longer remounts (2026-07-04).** Root-caused the "text-edit deactivates after a commit": every in-session `openDocumentBuffer` reload (text-edit, redact Apply, organize, merge, page-furniture) mints a **new documentId**, and `CasualPdf.tsx`'s `isLoaded ? <Viewer> : <spinner>` gate swapped the Viewer for the spinner during the brief reload → **unmounted + remounted the Viewer**, resetting its `useState` (textEditing, per-commit undo/redo stacks, active tool). Confirmed with mount logging (UNMOUNT→MOUNT with a new documentId each commit). Fix: a `ViewerHost` latch that, once the first document has loaded, **always renders the Viewer** (React reuses the instance across the changing `documentId` prop, preserving its state); the spinner shows only on first load. Verified (`verify-textedit-persist.mjs`): two consecutive text edits apply without re-activating the tool; `verify-drive` (redact/organize) + `-overlay`/`-unicode` still green. Also restores per-commit undo/redo, which the remount had been wiping each commit.
- **Option C (PR6, 2026-07-04) — Google Fonts coverage ~18 → ~30, incl. monospace.** Added 12 more `GOOGLE_FAMILIES` (verified jsdelivr paths, embed-checked): **5 monospace** — Roboto Mono, JetBrains Mono, Source Code Pro, IBM Plex Mono, Space Mono (there was no monospace GF before; code/technical PDFs use them) — plus Fira Sans, Manrope, Karla, Libre Franklin, Titillium Web, Barlow, Mukta. Anchored `^name$` regexes (so `robotomono` ≠ `roboto`). Still unresolved in the probe: IBM Plex Sans, PT Sans, Cabin.
- **Text-edit hardening + audit fixes (2026-07-02).** Full audit pass: 10 fixes shipped: onBlur editBusy guard (C-2), mode→View preserves in-progress edits via refs (C-4), editError/editNote clears on doc change (H-4), ⌘F blocked inside `<input>` (H-5), redact-from-selection and redact-search-matches Y-flip corrected (H-7/H-8), stable `id` on RedactRect (M-1), Apply gated on editBusy (M-2), dark-mode input text color (M-3/M-4), onReady fires once per activation (M-6), duplicate preloadPdfium removed (L-9). **Font fail-closed:** `editTextRun` now returns `{ bytes, substituted }`; forces standard-font substitution for subset fonts (`/^[A-Z]{6}\+/` name prefix per ISO 32000 §9.6.4) even for same-char edits; banner shows "Font changed to a standard substitute…" when this occurs; `PdfTextRun.fontSubsetted` exposed for tooltip.
- **Phase 5 page furniture SHIPPED (2026-07-02).** `packages/pdf-sdk/src/page-furniture.ts` (lazy-loaded via `/page-furniture` subpath export): **addWatermark** (diagonal text overlay, opacity/rotation/size/color), **addHeaderFooter** (left/center/right bands top+bottom, `{page}` / `{pages}` / `{date}` variables, skip-first-page), **addBatesNumbers** (sequential stamps at any corner, prefix/digits/position). App: Menu → "Watermark / Header / Bates…" opens tabbed `PageFurnitureDialog.tsx`; result flows through `onDocumentReplaced` → version undo stack + autosave. pdf-lib shared async chunk (~437 KB gz 181 KB, same total as before).
- **UX polish pass (2026-07-02).** Visible Open/Download quick-action buttons in the appbar; dirty indicator (orange dot); fast CSS tooltip on rail buttons (220ms delay, right side); bottom bar placed in-flow (no page overlap); selection toolbar hidden while search is open; placement banners moved to `bottom: 68px`; rail scrollbar visible (thin 4px track); drag-and-drop PDF opening. See commits `2fb09ba`–`27c9052`.
- **Per-commit text-edit undo/redo (2026-07-02).** `chrome.tsx`: each `commitTextEdit` pushes the pre-commit bytes onto `textEditUndoStackRef` (capped at 20). `apiRef.current.undo()/redo()` and `canUndo()/canRedo()` route through this stack while `textEditing` is true, falling back to the annotation history plugin otherwise. Stacks cleared on tool exit. Both toggling text edit and the mode→view teardown clear the stacks. Verified: types pass, all 9 headless tests green.
- **Welcome/empty state (2026-07-02).** App starts with `src = null`; welcome screen shows logo, drag-drop zone, "Open PDF" and "Try sample" actions. Mode segmented control and Download button are hidden until a document is open. Menu items that need a doc (`Download`, `Print`, `Digitally sign…`, `Watermark…`) are disabled. `?src=` URL param bypasses the welcome screen (render-parity CI, headless driver). Recovery banner still shows over the welcome screen.
- **Insert / Merge PDF shipped (2026-07-02).** File → "Insert PDF…" appends another PDF's pages at the end of the active document. Implementation: `packages/pdf-sdk/src/merge.ts` (`mergePdfs(primary, secondary, options)`, lazy-loads pdf-lib same as redact/page-furniture) exposed as `@casualoffice/pdf/merge` SDK subpath; app reads current bytes via `api.getBytes()`, imports the secondary file via a second hidden `<input type="file">`, calls `mergePdfs`, routes result through `onDocumentReplaced` → version undo stack + autosave. The merge chunk is only 0.77 KB gz (pdf-lib is already in a shared chunk). Supports `'append'` (default) or `'prepend'` position + optional page-index filter. Verified headless (`verify-merge.mjs`): 1-page sample + 3-page multi.pdf → 4-page merged doc, dirty flag set. Typechecks green.
- **Code-review fix pass + CI hardening (2026-07-03/04, PR #8).** A three-agent review (app shell / viewer chrome / engine modules) drove a batch of UX + functional fixes, all verified (typecheck, build, headless drivers) and merged to `main`. **Trust/correctness:** the "Signed" badge no longer matches the generic `/Type/Annot` (every PDF has annotations) — narrowed to stamp subtypes; the `page-furniture.ts` "incremental/append-only" comment was false (pdf-lib `save()` is a full rewrite that invalidates prior signatures) and is corrected; text-edit + signing init singletons no longer cache a *rejected* promise forever (stayed dead until reload); font-substitution compares against the whole logical run, not just the primary text object. **Editor state:** editing tools (redact / insert-image / sign / organize / text-edit) now mutually exclude (no stacked layers / leaked `editBytes`); annotation shortcuts are scoped out of redact/image/text-edit; a lost-text-edit blur-commit race is fixed (synchronous in-flight guard + pending queue); search highlights clear on find-bar close. **Memory:** version undo/redo stacks capped (blob-URL leak); `geoCache` bounded. **a11y (WCAG 2.2):** keyboard-operable redaction (Enter adds a centered mark, arrows move, +/− resize, Delete removes, focus rings), MenuBar ARIA keyboard nav (arrow/Home/End + focus restore), dialog focus traps + restore for Sign/PageFurniture; print tab opened in-gesture (pop-up-blocker safe). **CI:** `deploy-pages.yml` retries once on the transient `actions/deploy-pages` "try again later" backend failure (dependency-free). Reviewer finding "doc-swap teardown" was a **false positive** (the app remounts via `key={src}`; the proposed fix would have broken text-edit). **`/Rotate` page furniture:** watermark/header/Bates now place in displayed ("visual") space honoring `/Rotate` + MediaBox origin, so they're correct in **spec-compliant external viewers** (Acrobat/Preview/Chrome) — the target for downloaded PDFs; geometry verified for all four rotations. NOTE the app's own viewer renders pages **native** (ignores `/Rotate`), so a furnished rotated PDF looks rotated if re-opened in-app — a known consequence, kept because furniture PDFs target external viewers (user decision 2026-07-04). Deferred (cosmetic/large): rail Undo/Redo enable-state reactivity; making the app viewer honor `/Rotate` on display.
- **AI features SHIPPED — "Ask this PDF" + agentic DocOps + MCP (2026-07-05/06).** A full AI surface, **two-mode only** (locked decisions #5/#6/#9): **Desktop** (Tauri → `docops_llm_call` → local llama.cpp; model chosen in the desktop app) and **Collab** (WS `/api/ai` → provider from the collab server env). **Provider/model config is NOT a casual_pdf concern** — no client provider picker (user was firm on this). All under `packages/pdf-sdk/src/ai/` + see [`docs/AI.md`](./docs/AI.md).
  - **Chat panel** (`AiPanel.tsx`, `loop.ts` `runDocOpsTurn`, `transport.ts`): streaming, processing indicator, Stop (preserves partial), Clear, quick prompts, design-token UI. Transports are `drivesLoop=true` (server/desktop hold the tool loop); `transport.available()` drives an honest "AI unavailable" state on web (no collab URL → no backend, by design). CollabTransport has an inactivity watchdog (no eternal "Thinking…").
  - **Tool bridge** (`bridge.ts` `PdfOpsBridge` + `catalog.ts` `PDF_CATALOG`/`PDF_SYSTEM_PROMPT`) over the imperative `CasualPdfApi`: `get_document_info` (structure only), **`get_document_text`** (whole-doc text → summaries), `get_page_text`, `goto_page`, **`search_document`** (hybrid retrieval), **`highlight_source`** (source-span highlight), **`detect_pii`** (whole-doc or per-page, optional `types` filter), **`mark_redaction`** (contextual PII), **`list_form_fields`**/**`fill_form`**. Catalog is name-sorted (a test enforces it).
  - **RAG** (`retrieve.ts`): BM25 + optional **dense/hybrid** (cosine + Reciprocal Rank Fusion) when the runtime supplies `CasualPdfApi.embedTexts` — the embedding *model* runs in the desktop worker / collab server, NOT bundled in the client (falls back to BM25). **Citations** (`cite.ts`): page refs + ranges → clickable jump-to-source.
  - **PII redaction** (`pii.ts`): **50 structured types** with checksums (Luhn, Verhoeff/Aadhaar, IBAN mod-97, ABA, NHS, CPF, BSN, DNI, ISIN); the AI marks contextual PII (names/companies/places) via `mark_redaction`. **Marks only** — the user Applies (secure flatten); `detect_pii` returns type counts, never values; adding marks auto-switches View→Edit so they're reviewable.
  - **MCP server** (`src/mcp/{server,handlers}.ts`, `@modelcontextprotocol/sdk`): headless pure-bytes stdio server — **9 tools** (merge, watermark, header/footer, bates, sign, verify, detect_pii, list/fill form). Self-contained dist bin via `pnpm --filter @casualoffice/pdf build:mcp` → `dist/mcp/server.mjs` (plain `node`, `casual-pdf-mcp` bin).
  - **Tested + CI-gated:** 45 unit (`verify-ai-engine.test.ts`) + MCP registry/handlers/stdio (`verify-mcp*`) in the new **`unit-tests`** job; AI browser E2Es (`verify-ai-{panel,summarize,highlight,redact}.mjs`) in **`editor-tests`**. A deep-analysis pass (two audit agents) fixed summarize + tool gaps + redaction visibility + honest transport + citation ranges + timeouts.
  - **Cross-repo runtime pending** (held until desktop build settles): the desktop worker's **embed command** (to light up hybrid RAG end-to-end) and the MCP **"drive the open document"** bridge + consume-MCP via collab `callLlm`.
- **Redaction "Keep text" (region-only) mode SHIPPED (2026-07-06).** User report: after redaction you can't edit/select text. Root cause (not a bug): the secure flatten replaces the entire redacted PAGE with an image (`redact.ts` `buildRedactedPdf` — `drawImage` over the whole MediaBox), so that page loses ALL text; on a 1-page doc that's everything. **User decision (2026-07-06): add a region-only "Keep text" mode** alongside the secure flatten. `redact.ts` `buildCoveredPdf(srcBytes, marks)` draws OPAQUE black boxes over the marked regions (rotation/origin-aware via the exported `page-furniture.ts` `visualPlacer`; mapped rect is axis-aligned for all quarter rotations → bounding-box, no rectangle rotation) and leaves the rest of the page's content stream intact → its other text stays selectable/editable. **TRADE-OFF, disclosed in the confirm dialog: this is a VISUAL cover, NOT true removal** — the covered text remains in the bytes and could be extracted; "Remove (secure)" (the default flatten) stays for true UX-S5 removal. `chrome.tsx`: a **Remove (secure) / Keep text** toggle in the redaction confirm dialog; `applyRedactions` branches on `redactMode` (default `'remove'`). Tested: `verify-redact-cover.mjs` (Node — text PRESERVED on the covered page + geometry kept on `/Rotate`+offset pages, in `unit-tests` CI) and `verify-redact-cover-ui.mjs` (browser — Keep-text redaction on page 1 → page 1 text still editable in-app + still in the downloaded bytes, in `editor-tests` CI). Secure flatten regression (`verify-redact-geom`, `verify-ai-redact`) still green (UX-S5 removal intact).
- **Open follow-up:** search re-index after `openDocumentBuffer` (text search returns 0 during text-edit sessions; works after exiting). Collaboration/Share stays **deprioritized**.
